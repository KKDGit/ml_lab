package com.escalatesoft.macros

import org.scalatest.{FunSpec, Matchers, SeveredStackTraces}

class Module14 extends FunSpec with Matchers with SeveredStackTraces {

  describe ("A function 2 macro") {
    // extend the FunctionMacros definition (under src/main/scala) to add handling of a Function2 to the
    // Function1 handling already there.
    // then use the tests in FunctionMacrosSpec to provide tests here that exercise your macro.
    // Make the compiler generate a warning if the macro name ends up being more than 100 characters, like for
    // the function 1 implementation.

  }

}
